# Pinned Mermaid test corpus

This directory is the repository-owned Mermaid corpus used by automated tests. It contains the `.mmd` examples needed for parser, adapter, class-diagram, preview, and multi-diagram baseline coverage.

Do not point tests at planning, brief, or documentation directories. Refresh this corpus deliberately when changing the pinned Mermaid reference version or test scope.
